[project]
name = "prime-agent-skill-prime-mcp"
version = "0.1.0"
description = "Generic host-owned MCP broker skill for Prime Agent."
requires-python = ">=3.10"
dependencies = ["prime-agent-runtime"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/prime_mcp"]
